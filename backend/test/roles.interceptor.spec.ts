import { RolesInterceptor } from "src/interceptors/roles.interceptor";


describe('RolesInterceptor', () => {
  it('should be defined', () => {
    expect(new RolesInterceptor()).toBeDefined();
  });
});
